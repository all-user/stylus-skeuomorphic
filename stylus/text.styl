draw_text($vars)
  val = 0px -1px 0px $vars.bottom_shadow,
    0px -2px 0px $vars.indirect_light,
    0px -3px 0px $vars.middle_tone,
    0px 1px 0px $vars.indirect_light,
    0px 3px rgba(0,0,0,.2);
  return { text-shadow: val }
