$duration = 100ms
$texture = 'image'
$texture_param ?= '../../images/pattern.png'
$image_size ?= '300px 300px'

// body background color
$bg_color = #ccd

// $vignette_noise: 0..1
$vignette_noise = .3

// $diffusion: 0..1
$diffusion = .1

// top_reflection
$top_blend = .25
$top_alpha = .8
$bottom_blend = .15
$bottom_alpha = .5
